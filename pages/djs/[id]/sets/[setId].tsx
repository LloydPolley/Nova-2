import classNames from "classnames/bind";
import PageWrapper from "../../../../components/PageWrapper";

// const cx = classNames.bind(style);

function djsetid() {
  return (
    <PageWrapper>
      <h1>DJs sets id</h1>
    </PageWrapper>
  );
}

export default djsetid;
